@{
    Layout = null;
}

<ul id="sideManu" class="nav nav-tabs nav-stacked">
    @foreach (var category in Model)
    {
        <li class="subMenu open"><a> @category.Name </a>
            @if(category.InverseParents != null && category.InverseParents.Count > 0)
            {
                <ul>
                @foreach (var subcategory in category.InverseParents)
                {
                    @if(subcategory.Status)
                    {
                        <li><a class="active" href="products.html"><i class="icon-chevron-right"></i>@subcategory.Name </a></li>
                    }
                }
                </ul>               
            }
        </li>
    }    
</ul>